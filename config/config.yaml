defaults:
  - model: dqn        # choose dqn or rainbow
  - train: base
  - env: hopper
  - _self_

seed: 42

paths:
  run_dir: ${hydra:runtime.output_dir}
  log_dir: ${paths.run_dir}/logs
  checkpoint_dir: ${paths.run_dir}/checkpoints
  video_dir: ${paths.run_dir}/videos

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
