\section{Proximal Policy Optimization (PPO)}
\label{sec:ppo}

\subsection{Algorithm Overview}

Proximal Policy Optimization (PPO)~\cite{schulman2017ppo} is an on-policy policy gradient method that directly optimizes a stochastic policy \(\pi_\theta(a|s)\)
 by maximizing the expected discounted return. To prevent destructively large policy updates, PPO introduces a clipped surrogate objective:
\begin{equation}
    \mathcal{L}^{\text{CLIP}}(\theta) = \mathbb{E}_t \left[ \min\left( r_t(\theta)\hat{A}_t,\; \text{clip}(r_t(\theta), 1-\varepsilon, 1+\varepsilon)\hat{A}_t \right) \right],
\end{equation}
where \(r_t(\theta) = \nicefrac{\pi_\theta(a_t|s_t)}{\pi_{\theta_\text{old}}(a_t|s_t)}\) is the probability ratio between the current and old policy, \(\hat{A}_t\)
 is an estimate of the advantage function, and \(\varepsilon\) is the clipping threshold. This formulation effectively constrains policy updates to a trust region
  without requiring second-order optimization.

PPO jointly optimizes a combined loss that also includes a value function term and an entropy bonus to encourage exploration:
\begin{equation}
    \mathcal{L}(\theta) = \mathcal{L}^{\text{CLIP}}(\theta) - c_1 \mathcal{L}^{\text{VF}}(\theta) + c_2 \mathcal{H}[\pi_\theta],
\end{equation}
where \(c_1\) and \(c_2\) are coefficients controlling the relative contribution of the value loss and the entropy bonus respectively. Advantages are estimated using
 Generalized Advantage Estimation (GAE)~\cite{schulman2018gae}, which balances bias and variance through a parameter \(\lambda\):
\begin{equation}
    \hat{A}_t = \sum_{l=0}^{\infty} (\gamma\lambda)^l \delta_{t+l}, \quad \delta_t = r_t + \gamma V(s_{t+1}) - V(s_t).
\end{equation}

Unlike DQN, PPO operates natively in continuous action spaces by parameterizing the policy as a Gaussian distribution over actions, making it directly applicable
 to continuous control tasks without any discretization.

\subsection{Hopper: Learning from Pixels}
\label{sec:ppo_hopper_results}

We validate PPO on the same Hopper task described in the previous lab excercise, maintaining identical environment and reward configurations to enable a fair
 comparison with BDQN.

\textbf{Environment Configuration:}
The agent observes $64 \times 64$ pixel inputs with a frame stack of $4$. We deploy $30$ parallel environments to increase data throughput and stabilize advantage
 estimation. The \texttt{healthy\_angle\_range} is widened to $[-0.4, 0.4]$ to encourage diverse state exploration early in training.

\textbf{Reward Configuration:}
The reward shaping follows the same formulation as before, targeting a forward velocity of $1.5$ units/s with $w_{\text{vel}} = 1.0$, a
 control smoothness penalty of $w_{\text{ctrl}} = 0.001$, and a healthy survival bonus of $w_{\text{healthy}} = 1.0$.

\textbf{Algorithm Configuration:}
PPO is trained for \todo{X} environment frames. Each rollout collects \todo{N} steps per parallel environment before performing \todo{K} epochs of minibatch updates. 
The clipping threshold is set to $\varepsilon = \todo{x}$, the GAE parameter to $\lambda = \todo{x}$, and the discount factor to $\gamma = \todo{x}$. The policy and 
value networks share a convolutional encoder, with separate MLP heads for the policy mean and value estimate. Optimization uses Adam with a learning rate of \todo{lr}, 
optionally with linear decay.

% --- HOPPER RESULTS PLOT ---
\begin{figure*}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/ppo_hopper_reward_speed_x.pdf}
    \caption{Training dynamics of PPO on the Hopper environment. (smoothed with an exponential moving average).}
    \label{fig:ppo_hopper_results}
\end{figure*}
% ---------------------------

\todo{Describe convergence behavior here: does the agent reach the target velocity? How does sample efficiency compare to BDQN? Note any instabilities or failure modes
 observed during training.}

\subsection{Humanoid: Scaling to High-Dimensional Control}
\label{sec:ppo_humanoid_results}

Following the Hopper experiments, we scale PPO to the Humanoid environment. Unlike BDQN, PPO avoids the combinatorial action-space explosion by directly outputting a
 continuous Gaussian policy over all joint torques, making it inherently better suited to high-dimensional continuous control.

\textbf{Environment Tweaks:}
We apply the same morphological simplifications as in the previous lab excercise: the Z-axis rotations of the abdomen and hips are frozen, all six arm joints
 are disabled, and the observation resolution is increased to $96 \times 96$ pixels to capture the finer postural nuances of the humanoid body.

\textbf{Algorithm Configuration:}
\todo{Describe the specific hyperparameter configuration used for Humanoid: number of parallel envs, rollout length, number of update epochs, learning rate, clipping
 threshold, entropy coefficient, and any curriculum or reward shaping adjustments relative to Hopper.}

% --- HUMANOID RESULTS PLOT ---
\begin{figure}[htbp]
    \centering
    \todo{Insert plot: episodic reward and/or gait metrics over training steps for PPO on Humanoid.}
    % \includegraphics[width=\linewidth]{ppo_humanoid_results.pdf}
    \caption{Training dynamics of PPO on the Humanoid environment.}
    \label{fig:ppo_humanoid_results}
\end{figure}
% ----------------------------

\todo{Describe results here: did PPO successfully learn to walk? How many frames were required? Compare qualitatively and quantitatively to the BDQN Humanoid failure.
 Discuss whether the continuous action space parameterization provided a meaningful advantage.}